{
  "name": "@shader-studio/mcp",
  "version": "0.1.0",
  "description": "MCP server that lets Codex, Claude Code, Cursor, and other MCP clients drive a locally-open Shader Studio tab.",
  "keywords": [
    "glsl",
    "mcp",
    "model-context-protocol",
    "shader-studio",
    "shaders"
  ],
  "homepage": "https://github.com/antelm-dev/shader-studio/tree/master/tools/mcp",
  "bugs": "https://github.com/antelm-dev/shader-studio/issues",
  "license": "Apache-2.0",
  "author": "Adel Terki",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antelm-dev/shader-studio.git",
    "directory": "tools/mcp"
  },
  "bin": {
    "shader-studio-mcp": "./dist/server.mjs"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rollup -c",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && vitest run",
    "pack:tarball": "pnpm run build && node -e \"require('node:fs').mkdirSync('dist-pack',{recursive:true})\" && npm pack --pack-destination ./dist-pack",
    "verify": "tsx scripts/verify-bridge.ts"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@rollup/plugin-commonjs": "^28.0.2",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^16.0.0",
    "@rollup/plugin-typescript": "^12.1.2",
    "@shader-studio/shared": "workspace:*",
    "@types/node": "^20.17.19",
    "@types/ws": "^8.18.1",
    "rollup": "^4.30.1",
    "tsx": "^4.23.1",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8",
    "ws": "^8.21.0",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || >=26.0.0"
  },
  "nx": {
    "targets": {
      "build": {
        "outputs": [
          "{projectRoot}/dist"
        ]
      },
      "dev": {
        "cache": false,
        "persistent": true
      },
      "pack:tarball": {
        "outputs": [
          "{projectRoot}/dist-pack"
        ]
      },
      "verify": {
        "cache": false
      }
    }
  }
}
